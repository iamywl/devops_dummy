apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: review-service
  namespace: ecommerce
  labels:
    app: review-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: review-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
