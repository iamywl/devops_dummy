apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecommerce

namePrefix: dev-

commonLabels:
  environment: dev

resources:
  - ../../base

patches:
  - path: resource-patches.yaml
    target:
      kind: Deployment
  - path: dev-config.yaml
